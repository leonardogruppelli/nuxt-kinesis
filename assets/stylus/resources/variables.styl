$black = #3f3d56
$grey = #d0cde1
$blue = #6c63ff

colors = { 
  black: $black,
  grey: $grey
  blue: $blue
}

for key, value in colors
  .bg-{key}
    background-color value

  .c-{key}
    color value